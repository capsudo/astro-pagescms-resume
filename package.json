{
  "name": "astro-pagescms-resume",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Astro resume website using content editable through Pages CMS. Also updates social-bios, GitHub \"profile\" and blog's About page.",
  "scripts": {
    "dev": "ASTRO_TELEMETRY_DISABLED=1 astro dev",
    "build": "ASTRO_TELEMETRY_DISABLED=1 astro check && ASTRO_TELEMETRY_DISABLED=1 astro build",
    "preview": "ASTRO_TELEMETRY_DISABLED=1 astro preview",
    "github:scope": "gh auth refresh -h github.com -s workflow",
    "netlify:login": "netlify login",
    "netlify:link": "netlify init",
    "netlify:admin": "netlify open",
    "generate:technology-icons": "node scripts/generate-technology-icon-assets.mjs",
    "generate:markdown": "node scripts/generate-markdown-files.mjs",
    "generate:blog-data": "node scripts/generate-blog-about-data.mjs"
  },
  "dependencies": {
    "@lucide/astro": "^1.20.0",
    "@tailwindcss/vite": "^4.3.1",
    "astro": "latest",
    "tailwindcss": "^4.3.1"
  },
  "devDependencies": {
    "@astrojs/check": "latest",
    "typescript": "latest"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
